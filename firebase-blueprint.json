{
  "entities": {
    "NewsletterSubscription": {
      "title": "Newsletter Subscription",
      "description": "Information about a user's newsletter subscription.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "User's email address."
        },
        "subscribedAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the user subscribed."
        }
      },
      "required": ["email", "subscribedAt"]
    }
  },
  "firestore": {
    "/newsletter_subscriptions/{subscriptionId}": {
      "schema": "NewsletterSubscription",
      "description": "Collects email addresses for the newsletter."
    }
  }
}
